{
  "name": "matrix-voyager-bot",
  "version": "2.0.0",
  "license": "GPL-3.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/turt2live/matrix-voyager-bot.git"
  },
  "dependencies": {
    "amqplib": "^0.5.3",
    "commander": "^2.19.0",
    "config": "^3.0.1",
    "escape-string-regexp": "^1.0.5",
    "js-yaml": "^3.12.0",
    "matrix-bot-sdk": "^0.2.1",
    "matrix-js-snippets": "^0.2.8",
    "mkdirp": "^0.5.1",
    "random-string": "^0.2.0"
  },
  "devDependencies": {
    "@types/node": "^8.10.39",
    "codelyzer": "^4.5.0",
    "rimraf": "^2.6.2",
    "typescript": "^3.2.2"
  },
  "scripts": {
    "generate-registration": "npm run build && node lib/index.js --generate-registration voyager-registration.yaml",
    "lint": "tslint --project ./tsconfig.json -t stylish",
    "build": "rimraf lib && npm run build:bot",
    "build:bot": "tsc",
    "start:appservice": "npm run build && npm run run:appservice",
    "start:room_handler": "npm run build && npm run run:room_handler",
    "run:appservice": "node lib/index.js -w appservice",
    "run:room_handler": "node lib/index.js -w room_handler"
  },
  "engines": {
    "node": ">= 8.0.0",
    "npm": ">= 6.0.0"
  }
}
